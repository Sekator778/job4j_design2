create table if not exists userspamer (
                                     id serial primary key,
                                     name varchar(88),
                                     email varchar(99)
);